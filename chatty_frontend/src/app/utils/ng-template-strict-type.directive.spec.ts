import { NgTemplateStrictTypeDirective } from './ng-template-strict-type.directive';

describe('NgTemplateStrictTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new NgTemplateStrictTypeDirective();
    expect(directive).toBeTruthy();
  });
});
